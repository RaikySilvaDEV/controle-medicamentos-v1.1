import React,{useEffect,useMemo,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';
type Role='patient'|'caregiver';type Unit='min'|'hour'|'day';type Med={id:string;name:string;type:string;interval:number;unit:Unit;start:string|null;paused:boolean;relatedId:string|null;offset:number;note:string};type Event={id:string;patientId:string;medId:string;at:string;by:string;kind:'used'|'administered';scheduledAt:string|null};type User={id:string;name:string;email:string;password:string;role:Role;onboardingCompleted:boolean;patientId:string|null;shareCode:string|null};type Store={version:string;users:User[];patients:{id:string;name:string}[];medications:Record<string,Med[]>;events:Event[];settings:{sound:boolean;notifications:boolean}};
const KEY='cm-v11-store',CHAN='cm-v11-realtime';const uid=()=>crypto.randomUUID();
const seedMeds:Med[]=[{id:'med-pred',name:'Acetato de prednisolona',type:'Colírio',interval:2,unit:'hour',start:null,paused:false,relatedId:null,offset:0,note:''},{id:'med-moxi',name:'Cloridrato de moxifloxacino',type:'Colírio',interval:3,unit:'hour',start:null,paused:false,relatedId:null,offset:0,note:''},{id:'med-dorz',name:'Cloridrato de dorzolamida',type:'Colírio',interval:12,unit:'hour',start:null,paused:false,relatedId:null,offset:0,note:''},{id:'med-brim',name:'Tartarato de brimonidina',type:'Colírio',interval:12,unit:'hour',start:null,paused:false,relatedId:'med-dorz',offset:7,note:''}];
const fresh=():Store=>({version:'1.1.0',users:[],patients:[],medications:{},events:[],settings:{sound:true,notifications:false}});
async function load():Promise<Store>{const s=localStorage.getItem(KEY);if(s)return JSON.parse(s);const[u,m]=await Promise.all([fetch('/data/users.json').then(r=>r.json()),fetch('/data/medications.json').then(r=>r.json())]);const x=fresh();x.users=u.users.map((a:any)=>({...a,patientId:a.role==='patient'?a.id:'patient-demo',shareCode:a.role==='patient'?'DEMO-1234':null}));x.patients=[{id:'patient-demo',name:'Paciente Demo'}];x.medications['patient-demo']=m.medications.map((a:any)=>({...a,unit:a.unit==='minutes'?'min':a.unit==='hours'?'hour':'day',start:null,paused:false,relatedId:a.relatedId||null,offset:a.offsetMinutes||0,note:a.note||''}));localStorage.setItem(KEY,JSON.stringify(x));return x}
function save(x:Store){localStorage.setItem(KEY,JSON.stringify(x));try{const c=new BroadcastChannel(CHAN);c.postMessage(Date.now());c.close()}catch{}}
function step(m:Med){return m.interval*(m.unit==='min'?60000:m.unit==='hour'?3600000:86400000)}function nextDose(m:Med,meds:Med[],now:number){if(!m.start||m.paused||m.interval<=0)return null;let t=Date.parse(m.start);if(m.relatedId){const r=meds.find(x=>x.id===m.relatedId);if(r?.start)t=Date.parse(r.start)+m.offset*60000}const s=step(m);if(!Number.isFinite(t)||!Number.isFinite(s)||s<=0)return null;while(t<=now)t+=s;return t}const fmt=(t:number)=>new Date(t).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});const countdown=(t:number,n:number)=>{const x=Math.max(0,t-n),h=Math.floor(x/3600000),m=Math.floor(x%3600000/60000);return x<10000?'Agora':h?`Em ${h}h ${m}min`:m?`Em ${m}min`:`Em ${Math.floor(x/1000)}s`};
function sound(){try{const A=window.AudioContext||(window as any).webkitAudioContext;const c=new A(),o=c.createOscillator(),g=c.createGain();o.frequency.value=880;g.gain.value=.08;o.connect(g);g.connect(c.destination);o.start();setTimeout(()=>{o.stop();c.close()},700)}catch{}}
function Auth({onLogin}:{onLogin:(u:User)=>void}){const[reg,setReg]=useState(false),[email,setEmail]=useState(''),[pass,setPass]=useState(''),[name,setName]=useState(''),[err,setErr]=useState('');const submit=async()=>{const d=await load();if(reg){if(!name||!email||pass.length<6)return setErr('Preencha os campos; senha mínima de 6 caracteres.');if(d.users.some(u=>u.email.toLowerCase()===email.toLowerCase()))return setErr('E-mail já cadastrado.');const id=uid(),u:User={id,name,email,password:pass,role:'patient',onboardingCompleted:false,patientId:id,shareCode:`CM-${Math.floor(100000+Math.random()*900000)}`};d.users.push(u);d.patients.push({id,name});d.medications[id]=seedMeds.map(x=>({...x}));save(d);localStorage.setItem('cm-v11-session',id);onLogin(u)}else{const u=d.users.find(x=>x.email.toLowerCase()===email.toLowerCase()&&x.password===pass);if(!u)return setErr('E-mail ou senha inválidos.');localStorage.setItem('cm-v11-session',u.id);onLogin(u)}};return <div className="auth"><div className="logo">💊</div><h1>Controle de Medicamentos</h1><p>Organize seus horários e acompanhe quem você ama.</p>{reg&&<input placeholder="Nome completo" value={name} onChange={e=>setName(e.target.value)}/>}<input placeholder="E-mail" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Senha" type="password" value={pass} onChange={e=>setPass(e.target.value)}/>{err&&<div className="error">{err}</div>}<button onClick={submit}>{reg?'Criar conta':'Entrar'}</button><button className="ghost" onClick={()=>{setReg(!reg);setErr('')}}>{reg?'Já tenho uma conta':'Criar uma conta'}</button><div className="demo"><b>Contas de teste</b><br/>Paciente: paciente@demo.local / demo123<br/>Acompanhante: acompanhante@demo.local / demo123</div></div>}
function Onboarding({user,onDone}:{user:User;onDone:(u:User)=>void}){const[role,setRole]=useState<Role>(user.role),[code,setCode]=useState(''),[err,setErr]=useState('');const go=()=>{const raw=localStorage.getItem(KEY);if(!raw)return;const d:Store=JSON.parse(raw),u=d.users.find(x=>x.id===user.id)!;u.role=role;u.onboardingCompleted=true;if(role==='patient'){u.patientId=u.patientId||u.id;u.shareCode=u.shareCode||`CM-${Math.floor(100000+Math.random()*900000)}`}else{const p=d.users.find(x=>x.role==='patient'&&x.shareCode?.toUpperCase()===code.trim().toUpperCase());if(!p)return setErr('Código do paciente não encontrado.');u.patientId=p.patientId||p.id;u.shareCode=null}save(d);onDone(u)};return <div className="auth onboarding"><div className="logo">👋</div><h1>Como você vai usar?</h1><button className={role==='patient'?'selected':''} onClick={()=>setRole('patient')}>💊 Sou paciente<small>Organizar e registrar meus medicamentos</small></button><button className={role==='caregiver'?'selected':''} onClick={()=>setRole('caregiver')}>👨‍👩‍👦 Acompanhar familiar<small>Ver horários e administrar quando autorizado</small></button>{role==='caregiver'&&<input placeholder="Código do paciente" value={code} onChange={e=>setCode(e.target.value)}/>} {err&&<div className="error">{err}</div>}<button onClick={go}>Continuar</button></div>}
function App(){const[data,setData]=useState<Store|null>(null),[user,setUser]=useState<User|null>(null),[tab,setTab]=useState('home'),[now,setNow]=useState(Date.now()),[editing,setEditing]=useState<Med|null>(null),[manual,setManual]=useState<Med|null>(null),[alarm,setAlarm]=useState<{med:Med;t:number}|null>(null),[filter,setFilter]=useState('7');useEffect(()=>{load().then(d=>{setData(d);const sid=localStorage.getItem('cm-v11-session');if(sid)setUser(d.users.find(u=>u.id===sid)||null)})},[]);useEffect(()=>{const i=setInterval(()=>setNow(Date.now()),1000),fn=()=>load().then(setData);window.addEventListener('storage',fn);let c:BroadcastChannel|undefined;try{c=new BroadcastChannel(CHAN);c.onmessage=fn}catch{}return()=>{clearInterval(i);window.removeEventListener('storage',fn);c?.close()}},[]);
if(!data)return <div className="loading">💊<br/>Carregando...</div>;if(!user)return <Auth onLogin={setUser}/>;if(!user.onboardingCompleted)return <Onboarding user={user} onDone={setUser}/>;const patientId=user.patientId||user.id,patient=data.patients.find(p=>p.id===patientId),meds=data.medications[patientId]||[],events=data.events.filter(e=>e.patientId===patientId);const upcoming=useMemo(()=>meds.map(m=>({m,t:nextDose(m,meds,now)})).filter(x=>x.t!==null).sort((a,b)=>a.t!-b.t!),[meds,now]);const update=(fn:(x:Store)=>void)=>{const x=structuredClone(data);fn(x);save(x);setData(x);const u=x.users.find(a=>a.id===user.id);if(u)setUser(u)};const start=(m:Med,when=new Date())=>update(x=>{const z=x.medications[patientId]?.find(a=>a.id===m.id);if(z){z.start=when.toISOString();z.paused=false}});const record=(m:Med,kind:'used'|'administered',when=new Date())=>{const scheduled=nextDose(m,meds,when.getTime()-1000);if(events.some(e=>e.medId===m.id&&scheduled&&e.scheduledAt&&Math.abs(Date.parse(e.scheduledAt)-scheduled)<60000))return alert('Esta dose já foi registrada.');update(x=>x.events.push({id:uid(),patientId,medId:m.id,at:when.toISOString(),by:kind==='administered'?user.name:'Paciente',kind,scheduledAt:scheduled?new Date(scheduled).toISOString():null}))};const remove=(m:Med)=>{if(meds.some(x=>x.relatedId===m.id))return alert('Este medicamento está relacionado a outro. Edite a relação antes de excluir.');if(confirm('Tem certeza que deseja excluir este medicamento?'))update(x=>{x.medications[patientId]=x.medications[patientId].filter(a=>a.id!==m.id)})};const check=()=>{const u=upcoming[0];if(u?.t&&Math.abs(u.t-now)<1000&&alarm?.t!==u.t){setAlarm({med:u.m,t:u.t});if(data.settings.sound)sound();if('Notification'in window&&Notification.permission==='granted')new Notification('🔔 Hora do medicamento',{body:`Está na hora de usar: ${u.m.name}`})}};check();const active=meds.filter(m=>m.start&&!m.paused).length,waiting=meds.filter(m=>!m.start&&!m.paused).length,today=events.filter(e=>new Date(e.at).toDateString()===new Date(now).toDateString()).length;
return <div className="app"><header><div><small>CONTROLE DE</small><h1>💊 Medicamentos</h1><span className="who">{user.role==='caregiver'?'Acompanhando':'Paciente'}: {patient?.name||user.name}</span></div><button className="iconbtn" onClick={()=>{localStorage.removeItem('cm-v11-session');setUser(null)}}>Sair</button></header>{alarm&&<div className="alarm"><b>🔔 HORA DO MEDICAMENTO</b><span>{alarm.med.name}</span><div><button onClick={()=>{record(alarm.med,user.role==='caregiver'?'administered':'used');setAlarm(null)}}>✅ {user.role==='caregiver'?'Administrado':'Marcar usado'}</button><button className="ghost" onClick={()=>{setAlarm(null);setTimeout(sound,0)}}>⏰ Adiar 5 min</button></div></div>}
{tab==='home'&&<main><section className="hero"><small>PRÓXIMO MEDICAMENTO</small><h2>{upcoming[0]?.m.name||'Nenhum programado'}</h2><strong>{upcoming[0]?fmt(upcoming[0].t!):'—'}</strong><p>{upcoming[0]?countdown(upcoming[0].t!,now):'Defina o primeiro horário'}</p></section><div className="stats"><div><b>{meds.length}</b><small>Medicamentos</small></div><div><b>{active}</b><small>Ativos</small></div><div><b>{today}</b><small>Doses hoje</small></div></div><h3>⏰ Próximos horários</h3>{upcoming.slice(0,10).map(x=><div className="card row" key={x.m.id}><b>{fmt(x.t!)}</b><div><strong>{x.m.name}</strong><small>{countdown(x.t!,now)}</small></div><button onClick={()=>record(x.m,user.role==='caregiver'?'administered':'used')}>{user.role==='caregiver'?'💊 Administrar':'💊 Usei'}</button></div>)}{!upcoming.length&&<div className="empty">🕐 {waiting?`${waiting} medicamento(s) aguardando início.`:'Nenhuma dose programada.'}</div>}<div className="card share"><b>🔗 Acompanhamento familiar</b><p>{user.role==='patient'?'Seu código para compartilhar:':'Você está acompanhando este paciente.'}</p>{user.role==='patient'&&<strong>{user.shareCode}</strong>}</div></main>}
{tab==='meds'&&<main><div className="title"><h2>💊 Medicamentos</h2><button onClick={()=>setEditing({id:uid(),name:'',type:'Colírio',interval:1,unit:'hour',start:null,paused:false,relatedId:null,offset:0,note:''})}>＋ Adicionar</button></div>{meds.map(m=>{const n=nextDose(m,meds,now);return <div className="card med" key={m.id}><div className="medhead"><div><h3>{m.name}</h3><small>{m.type} · {m.interval}{m.unit==='hour'?'h':m.unit==='min'?' min':' dias'} · {m.paused?'⏸️ Pausado':m.start?'🟢 Ativo':'🕐 Aguardando início'}</small></div><span className="badge">{n?fmt(n):'—'}</span></div>{m.relatedId&&<small>🔗 {meds.find(x=>x.id===m.relatedId)?.name||'Medicamento removido'} +{m.offset} min</small>}{m.note&&<p className="note">{m.note}</p>}<div className="actions">{!m.start&&<button onClick={()=>start(m)}>▶️ Começar agora</button>}{m.start&&<button onClick={()=>setManual(m)}>🕐 Informar horário</button>}{m.start&&<button onClick={()=>record(m,user.role==='caregiver'?'administered':'used')}>💊 {user.role==='caregiver'?'Administrar':'Usei agora'}</button>}<button className="ghost" onClick={()=>update(x=>{const z=x.medications[patientId].find(a=>a.id===m.id);if(z)z.paused=!z.paused})}>{m.paused?'▶️ Retomar':'⏸️ Pausar'}</button><button className="ghost" onClick={()=>setEditing({...m})}>✏️ Editar</button><button className="danger" onClick={()=>remove(m)}>🗑️ Excluir</button></div></div>})}</main>}
{tab==='history'&&<main><div className="title"><h2>📋 Histórico</h2><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="1">Hoje</option><option value="2">Ontem</option><option value="7">7 dias</option><option value="30">30 dias</option><option value="all">Tudo</option></select></div>{events.filter(e=>filter==='all'||now-Date.parse(e.at)<=Number(filter)*86400000).slice().reverse().map(e=><div className="card row" key={e.id}><b>{fmt(Date.parse(e.at))}</b><div><strong>{meds.find(m=>m.id===e.medId)?.name||'Medicamento removido'}</strong><small>{new Date(e.at).toLocaleDateString('pt-BR')} · {e.kind==='administered'?'💊 Administrado':'✅ Usado'} · por {e.by}</small></div></div>)}{!events.length&&<div className="empty">Nenhuma dose registrada.</div>}</main>}
{tab==='settings'&&<main><h2>⚙️ Configurações</h2><div className="card settings"><button onClick={async()=>{if(!('Notification'in window))return alert('Notificações não são suportadas neste navegador.');const p=await Notification.requestPermission();update(x=>x.settings.notifications=p==='granted');alert(p==='granted'?'Notificações ativadas.':'Permissão não concedida.')}}>🔔 Ativar notificações</button><button onClick={()=>{sound();alert('Alarme testado.')}}>🔊 Testar alarme</button><button onClick={()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='controle_medicamentos_backup.json';a.click()}}>📤 Exportar dados</button><label className="filebtn">📥 Importar dados<input type="file" accept="application/json" onChange={e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const x=JSON.parse(String(r.result));save(x);setData(x);alert('Dados importados.')}catch{alert('JSON inválido.')}};r.readAsText(f)}}/></label><button className="danger" onClick={()=>{if(confirm('Limpar todo o histórico deste paciente?'))update(x=>x.events=x.events.filter(e=>e.patientId!==patientId))}}>🗑️ Limpar histórico</button><button className="ghost" onClick={()=>{if(confirm('Restaurar os quatro medicamentos iniciais?'))update(x=>x.medications[patientId]=seedMeds.map(a=>({...a})))}}>♻️ Restaurar medicamentos iniciais</button><p className="notice">🟢 V1.1 sincroniza abas/janelas do mesmo navegador via localStorage + BroadcastChannel. Para sincronizar celulares diferentes será necessário backend.</p><p className="notice">Este aplicativo é apenas um organizador de horários e não substitui orientação médica.</p></div></main>}
<nav><button className={tab==='home'?'active':''} onClick={()=>setTab('home')}>🏠<span>Início</span></button><button className={tab==='meds'?'active':''} onClick={()=>setTab('meds')}>💊<span>Medicamentos</span></button><button className={tab==='history'?'active':''} onClick={()=>setTab('history')}>📋<span>Histórico</span></button><button className={tab==='settings'?'active':''} onClick={()=>setTab('settings')}>⚙️<span>Configurações</span></button></nav>{editing&&<MedModal med={editing} meds={meds} onClose={()=>setEditing(null)} onSave={m=>{update(x=>{const a=x.medications[patientId],i=a.findIndex(z=>z.id===m.id);if(i>=0)a[i]=m;else a.push(m)});setEditing(null)}}/>}{manual&&<ManualModal med={manual} onClose={()=>setManual(null)} onSave={(date,time)=>{start(manual,new Date(`${date}T${time}`));setManual(null)}}/>}</div>}
function MedModal({med,meds,onClose,onSave}:{med:Med;meds:Med[];onClose:()=>void;onSave:(m:Med)=>void}){const[m,setM]=useState({...med});return <div className="modal"><div className="modalbox"><h2>{med.name?'✏️ Editar':'➕ Adicionar medicamento'}</h2><input placeholder="Nome" value={m.name} onChange={e=>setM({...m,name:e.target.value})}/><select value={m.type} onChange={e=>setM({...m,type:e.target.value})}>{['Comprimido','Cápsula','Colírio','Gotas','Pomada','Injeção','Vitamina','Outro'].map(x=><option key={x}>{x}</option>)}</select><div className="grid2"><input type="number" min="1" value={m.interval} onChange={e=>setM({...m,interval:Math.max(1,Number(e.target.value))})}/><select value={m.unit} onChange={e=>setM({...m,unit:e.target.value as Unit})}><option value="min">Minutos</option><option value="hour">Horas</option><option value="day">Dias</option></select></div><label>Medicamento relacionado</label><select value={m.relatedId||''} onChange={e=>setM({...m,relatedId:e.target.value||null})}><option value="">Nenhum</option>{meds.filter(x=>x.id!==m.id).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select>{m.relatedId&&<input type="number" min="0" placeholder="Atraso em minutos" value={m.offset} onChange={e=>setM({...m,offset:Math.max(0,Number(e.target.value))})}/>}<textarea placeholder="Observação" value={m.note} onChange={e=>setM({...m,note:e.target.value})}/><div className="actions"><button onClick={()=>m.name.trim()?onSave(m):alert('Informe o nome.')}>Salvar</button><button className="ghost" onClick={onClose}>Cancelar</button></div></div></div>}
function ManualModal({med,onClose,onSave}:{med:Med;onClose:()=>void;onSave:(date:string,time:string)=>void}){const[d,setD]=useState(med.start?new Date(med.start).toISOString().slice(0,10):new Date().toISOString().slice(0,10)),[t,setT]=useState(med.start?new Date(med.start).toTimeString().slice(0,5):new Date().toTimeString().slice(0,5));return <div className="modal"><div className="modalbox"><h2>🕐 Definir primeiro horário</h2><p>{med.name}</p><label>Data</label><input type="date" value={d} onChange={e=>setD(e.target.value)}/><label>Horário</label><input type="time" value={t} onChange={e=>setT(e.target.value)}/><div className="actions"><button onClick={()=>onSave(d,t)}>Salvar horário</button><button className="ghost" onClick={onClose}>Cancelar</button></div></div></div>}
createRoot(document.getElementById('root')!).render(<App/>);
